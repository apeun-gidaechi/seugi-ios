import SwiftUI

public struct UpdateNotificationView: View {
    
    private let id: Int
    
    init(id: Int) {
        self.id = id
    }
    
    public var body: some View {
        EmptyView()
    }
}
