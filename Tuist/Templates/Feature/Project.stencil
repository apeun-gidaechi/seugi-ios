import ProjectDescription
import ProjectDescriptionHelpers

let project = Project.makeFeature(
    target: .{{ name }},
    targets: [
        .feature(target: .{{ name }}, dependencies: [
            .featureInterface(target: .Base),
            .featureInterface(target: .{{ name }}),
        ]),
        .featureExample(target: .{{ name }}, dependencies: [
            .feature(target: .{{ name }})
        ]),
        .featureInterface(target: .{{ name }}, dependencies: [])
    ]
)
