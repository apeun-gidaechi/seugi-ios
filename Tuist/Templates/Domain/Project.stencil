import ProjectDescription
import ProjectDescriptionHelpers

let project = Project.makeDomain(
    target: .{{ name }},
    targets: [
        .domain(target: .{{ name }}, dependencies: [
            .domainInterface(target: .Base)
        ]),
        .domainInterface(target: .{{ name }}, dependencies: [])
    ]
)
